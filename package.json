{
  "name": "template_cypress",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:local": "cypress run --config baseUrl=http://localhost:3000"
  },
  "devDependencies": {
    "cypress": "^12.7.0",
    "mochawesome": "^7.1.3"
  }
}
